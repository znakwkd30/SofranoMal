<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.hs.dgsw.webclass01.Domain.MenuMapper">
    <select id="findall" resultType="kr.hs.dgsw.webclass01.Domain.Menu">
        select id, name from menu
    </select>

    <select id="findById" parameterType="Long" resultType="kr.hs.dgsw.webclass01.Domain.Menu">
        select id, name from menu
    </select>
</mapper>